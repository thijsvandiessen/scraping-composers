[project]
name = "composer-config"
version = "0.1.0"
description = "Central configuration settings for the scraper backend"
dependencies = [
    "pydantic-settings>=2.0.0",
]
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
