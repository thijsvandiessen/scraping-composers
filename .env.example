# Database connection (default: sqlite:///composers.db)
DATABASE_URL=

# Path to raw-data bucket (default: ./raw-data)
BUCKET_PATH=

# Contact email included in the HTTP User-Agent header sent to source APIs.
# Without this the agent string omits the contact part entirely.
INGEST_CONTACT_EMAIL=you@example.com
